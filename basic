package main
import "fmt"

func main(){
  fmt.Println("1+1")
  
  var name string = "Ayush"
  var naam = "Ayu"
  var age int = 25
  var ages = 25
  
  naam = "Iti"
  
  const (
    port= 3000
    host= "localhost"
    domain= "d4community"
  )
  
  fmt.Println(name)
  fmt.Println(age)
  fmt.Println(naam)
  fmt.Println(ages)
  fmt.Println(port, host, domain)
}
